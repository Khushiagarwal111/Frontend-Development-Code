//DOCUMENT OBJECT MODEL-DOM
//Document we have html-> we have HEAD & BODY
const grand = Array.from(document.getElementByClassName("grandpa"));
// const grand = document.getElementById("grandpaaid");
// const parent = document.getElementById("parentid");
// 
grand.forEach(function(element, index, array) {
            color);


        function color(val) {
            val.style.backgroundColor = "yellow";
        }
        // color(grand);
        color(parent) //
        const grand = document.quer("grandpaaid");